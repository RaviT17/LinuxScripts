#!/bin/bash
# check GPU CHECK on VDIs application


if [ -f /usr/bin/nvidia-smi ]
then

HOSTNAME=$(hostname -f)
NATCO=$(get_location)
cmd=`nvidia-smi --query-gpu=timestamp,name,pci.bus_id,driver_version,pstate,utilization.gpu,utilization.memory,memory.total,memory.free,memory.used --format=csv | tail -1`


echo "${HOSTNAME},${cmd}" >> /home/i0001314/myfiles/gpulog
fi

--